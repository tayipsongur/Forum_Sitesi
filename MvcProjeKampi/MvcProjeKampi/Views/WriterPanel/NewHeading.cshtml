@model EntityLayer.Concrete.Heading

@{
    ViewBag.Title = "NewHeading";
    Layout = "~/Views/Shared/_WriterLayout.cshtml";
}

<h2>NewHeading</h2>

@using (Html.BeginForm("NewHeading", "WriterPanel", FormMethod.Post))
{
    <div class="form-group">

        @Html.Label("Başlık Adı")
        @Html.TextBoxFor(x => x.HeadingName, new { @class = "form-control" })
        @Html.ValidationMessageFor(x => x.HeadingName)
        <br />
        @Html.Label("Kategori")
        @Html.DropDownListFor(x => x.CategoryID, (List<SelectListItem>)ViewBag.vlc, new { @class = "form-control" })

        <!-- Taşıma işlemlerinde ViewBag'den önce List<SelectListItem> Kullanıyoruz. vlc controller tarafında tuttuğumuz değere karşılık geliyor.'-->
        <br />
        

        <br />
        <button class="btn btn-success"> Başlık Oluştur </button>

    </div>
}

